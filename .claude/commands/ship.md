Stage all changes, create a descriptive commit (following the project's commit style with Co-Authored-By tag), then push to origin main.

Use `git status` and `git diff` to understand what changed, write a concise commit message, run `git add` on relevant files, `git commit`, then `git push`.
